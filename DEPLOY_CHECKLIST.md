# 📦 部署检查清单

**版本**：v1.1.0
**更新日期**：2026-03-23
**修复内容**：积分快照 + 周期容错

---

## 🔴 必须上传的云函数（6个）

### 1. ✅ checkin-submit
**修改内容**：
- 打卡记录保存条目快照（itemTitle, itemPoints）
- 打卡时检查周期有效性

**影响范围**：
- 所有打卡操作
- 核心数据完整性

**验证方法**：
```
1. 创建条目
2. 打卡
3. 查看数据库 checkin_record
4. 验证 itemTitle 和 itemPoints 字段存在
```

---

### 2. ✅ timer-dailyCheck
**修改内容**：
- 漏卡记录保存条目快照（itemTitle, itemPoints）

**影响范围**：
- 定时任务（23:59）
- 漏卡记录完整性

**验证方法**：
```
1. 手动触发 timer-dailyCheck
2. 查看生成的漏卡记录
3. 验证快照字段
```

---

### 3. ✅ settlement-getBalance
**修改内容**：
- 优先使用记录中的快照积分
- 兼容旧数据（无快照时查询条目）

**影响范围**：
- 积分统计
- PK页面显示

**验证方法**：
```
1. 调用 settlement-getBalance
2. 验证积分计算正确
3. 删除条目后再次调用，积分不变
```

---

### 4. ✅ settlement-getMissedPoints
**修改内容**：
- 优先使用记录中的快照积分
- 兼容旧数据

**影响范围**：
- 漏卡积分统计
- PK页面显示

**验证方法**：
```
1. 调用 settlement-getMissedPoints
2. 验证漏卡积分正确
```

---

### 5. ✅ user-login
**修改内容**：
- 登录时检查周期有效性
- 自动修复无效周期
- 尝试使用伙伴周期或创建新周期

**影响范围**：
- 所有用户登录
- 系统容错能力

**验证方法**：
```
1. 手动将用户的 currentPeriodId 设为无效值
2. 重新登录
3. 验证周期自动修复
4. 验证可以正常打卡
```

---

### 6. ✅ test-runner
**修改内容**：
- 新增4个测试用例
  - 打卡记录保存条目快照
  - 删除条目后积分不丢失
  - 漏卡记录也保存快照
  - 周期容错-无效周期自动修复
- 更新清理逻辑

**影响范围**：
- 测试覆盖率
- 质量保证

**验证方法**：
```
1. 云端测试 test-runner
2. 验证24个测试全部通过
3. 验证新测试用例正常执行
```

---

## 📋 部署步骤

### 第1步：上传云函数（20分钟）

```
微信开发者工具
→ 云开发
→ 云函数

依次上传以下云函数：
1. checkin-submit
2. timer-dailyCheck
3. settlement-getBalance
4. settlement-getMissedPoints
5. user-login
6. test-runner

选择：上传并部署-云端安装依赖
```

**注意事项**：
- ✅ 每个函数上传后等待部署完成
- ✅ 检查控制台是否有错误
- ✅ 确认版本号更新

---

### 第2步：运行自动化测试（5分钟）

```
云开发
→ 云函数
→ test-runner
→ 云端测试

输入: {}

等待执行完成（约30秒）
```

**预期结果**：
```json
{
  "success": true,
  "summary": {
    "total": 24,
    "passed": 24,
    "failed": 0,
    "passRate": "100%"
  }
}
```

**如果失败**：
- 查看详细日志
- 检查哪个测试失败
- 回滚对应云函数
- 修复后重新上传

---

### 第3步：手动验证核心功能（10分钟）

#### 测试1：积分快照验证

```
小程序操作：
1. 创建条目"测试快照"（10分）
2. 打卡1次
3. 查看本周积分（应为10分）
4. 删除"测试快照"条目
5. 再次查看本周积分（应仍为10分）✅

数据库验证：
1. 打开云开发控制台
2. 查看 checkin_record 集合
3. 找到刚才的打卡记录
4. 验证 itemTitle 和 itemPoints 字段存在
```

**通过标准**：
- ✅ 删除条目后积分不丢失
- ✅ 记录包含快照字段

---

#### 测试2：周期容错验证

```
数据库操作：
1. 打开云开发控制台
2. 找到自己的用户记录
3. 将 currentPeriodId 改为 "invalid_test_123"
4. 保存

小程序操作：
1. 重新登录小程序
2. 查看是否正常显示
3. 尝试打卡

数据库验证：
1. 再次查看用户记录
2. 验证 currentPeriodId 已被修复
```

**通过标准**：
- ✅ 登录后周期自动修复
- ✅ 可以正常打卡
- ✅ 无报错

---

#### 测试3：积分统计正确性

```
小程序操作：
1. 完成几次打卡
2. 查看PK页面
3. 验证积分正确
4. 查看统计页面
5. 验证历史数据正确
```

**通过标准**：
- ✅ PK页面积分正确
- ✅ 统计页面数据正确
- ✅ 无报错

---

### 第4步：监控运行状态（1天）

#### 关注指标

**错误监控**：
```
云开发控制台
→ 云函数
→ 日志
→ 查看错误日志
```

**关注点**：
- 是否有新的报错
- 周期自动修复频率
- 积分统计是否正常

**告警标准**：
- ❌ 出现大量报错
- ❌ 用户无法打卡
- ❌ 积分统计错误

---

## ✅ 验证检查表

### 部署验证

- [ ] 6个云函数全部上传成功
- [ ] test-runner 24个测试全部通过
- [ ] 无部署错误

### 功能验证

- [ ] 打卡记录包含快照字段
- [ ] 删除条目后积分不丢失
- [ ] 周期容错自动修复
- [ ] PK页面积分正确
- [ ] 统计页面数据正确

### 运行验证

- [ ] 无错误日志
- [ ] 无用户反馈问题
- [ ] 系统运行稳定

---

## 🔄 回滚方案

### 如果出现严重问题

**立即回滚步骤**：

1. **回滚云函数**
   ```
   微信开发者工具
   → 云函数
   → 右键云函数
   → 版本管理
   → 回滚到上一版本
   ```

2. **需要回滚的函数**：
   - checkin-submit
   - timer-dailyCheck
   - settlement-getBalance
   - settlement-getMissedPoints
   - user-login

3. **通知用户**：
   - 发布维护公告
   - 说明正在修复
   - 预计恢复时间

---

## 📊 数据迁移说明

### 无需数据迁移 ✅

**原因**：
- 新代码向后兼容
- 有快照使用快照
- 无快照查询条目（旧逻辑）

**旧数据处理**：
- ✅ 自动兼容
- ✅ 不影响功能
- ✅ 逐步迁移（新打卡自动带快照）

---

## 🎯 成功标准

### 部署成功的标志

1. ✅ 所有云函数上传成功
2. ✅ 测试全部通过（24/24）
3. ✅ 手动验证通过
4. ✅ 运行24小时无错误
5. ✅ 用户无反馈问题

### 可以进入下一阶段

如果以上全部满足，可以：
- 配置定时触发器
- 开始真实场景测试
- 准备正式上线

---

## 📝 部署记录

### 部署信息

- **部署人**：___________
- **部署时间**：___________
- **版本号**：v1.1.0
- **环境**：cloud1-9gtimm7r81602bae

### 验证结果

- [ ] 部署验证通过
- [ ] 功能验证通过
- [ ] 运行验证通过

### 签名确认

- **部署人签名**：___________
- **验证人签名**：___________
- **日期**：___________

---

## 🚀 下一步

部署成功后：

1. ✅ 配置定时触发器
   - timer-dailyCheck（每天23:59）
   - timer-weeklySettlement（周日23:59）

2. ✅ 真实场景测试
   - 邀请2-3对情侣测试
   - 收集反馈
   - 优化体验

3. ✅ 正式上线
   - 提交小程序审核
   - 发布上线
   - 运营推广

---

**准备好了就开始部署吧！** 🎉
